import Login from "@/components/Login/Login";
import React from "react";

const page = () => {
  return (
    <div className="bg-bgGradientFinish w-screen h-screen flex">
      <Login />
    </div>
  );
};

export default page;
